The code uses LangChain’s PyPDFLoader to read a PDF file

from langchain_community.document_loaders import PyPDFLoader
loader = PyPDFLoader('../../abc.pdf')
pdf_document = loader.load()
pdf_document

print(pdf_document[0].page_content)
